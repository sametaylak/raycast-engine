const TILE_SIZE = 64
const NUMBER_OF_ROWS = 11
const NUMBER_OF_COLS = 11
const SCREEN_WIDTH = NUMBER_OF_COLS * TILE_SIZE
const SCREEN_HEIGHT = NUMBER_OF_ROWS * TILE_SIZE
const RAY_WIDTH = 10
const NUMBER_OF_RAYS = SCREEN_WIDTH / RAY_WIDTH
const FOV_ANGLE = 60 * (Math.PI / 180)
const SCALE_FACTOR = 0.2
